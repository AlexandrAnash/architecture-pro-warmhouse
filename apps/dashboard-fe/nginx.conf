server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Только статика SPA. Все ручки идут через gateway (:8000) напрямую из браузера.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
